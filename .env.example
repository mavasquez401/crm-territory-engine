# Airflow Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control

# Airflow Webserver Secret Key
# Generate a new secret key using: python3 scripts/generate_secrets.py
# Or manually: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
AIRFLOW__WEBSERVER__SECRET_KEY=your-secret-key-here

# Airflow Fernet Key (for encrypting connection passwords)
# Generate using: python3 scripts/generate_secrets.py
# Or manually: python3 -c "import base64, secrets; print(base64.urlsafe_b64encode(secrets.token_bytes(32)).decode())"
AIRFLOW__CORE__FERNET_KEY=your-fernet-key-here

# Database Connection (if using PostgreSQL in production)
# For development, SQLite is used by default (configured in airflow.cfg)
# AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://user:password@localhost:5432/airflow

# SMTP Configuration (if using email notifications)
# AIRFLOW__SMTP__SMTP_USER=your-smtp-user
# AIRFLOW__SMTP__SMTP_PASSWORD=your-smtp-password
# AIRFLOW__SMTP__SMTP_HOST=smtp.gmail.com
# AIRFLOW__SMTP__SMTP_PORT=587
# AIRFLOW__SMTP__SMTP_MAIL_FROM=airflow@example.com
