# Environment Variables for CRM Territory Engine
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# Airflow Secret Key
# Generate a new secret key using: python -c "import secrets; print(secrets.token_urlsafe(32))"
AIRFLOW__WEBSERVER__SECRET_KEY=your-secret-key-here

# Database Configuration (if using PostgreSQL instead of SQLite)
# AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://user:password@localhost:5432/airflow

# SMTP Configuration (for email alerts)
# AIRFLOW__SMTP__SMTP_HOST=smtp.gmail.com
# AIRFLOW__SMTP__SMTP_PORT=587
# AIRFLOW__SMTP__SMTP_USER=your-email@example.com
# AIRFLOW__SMTP__SMTP_PASSWORD=your-app-password
# AIRFLOW__SMTP__SMTP_MAIL_FROM=airflow@example.com

# Fernet Key (for encrypting connection passwords in DB)
# Generate using: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# AIRFLOW__CORE__FERNET_KEY=your-fernet-key-here
